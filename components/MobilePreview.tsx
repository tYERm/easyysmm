import React from 'react';

// Этот файл не используется в EasySMM.
// Оставляем заглушку, чтобы не ломать сборку (TS Build).
const MobilePreview = () => {
  return null;
};

export default MobilePreview;